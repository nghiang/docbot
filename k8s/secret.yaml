# WARNING: Do NOT commit real secret values to git.
# Apply this file manually on the cluster after filling in real values:
#   kubectl apply -f k8s/secret.yaml
#
# Alternatively, use Sealed Secrets or External Secrets Operator for proper GitOps.
apiVersion: v1
kind: Secret
metadata:
  name: docbot-secret
  namespace: docbot
type: Opaque
stringData:
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
  POSTGRES_PASSWORD: "docbot123"
  MONGODB_PASSWORD: "docbot123"
  JWT_SECRET_KEY: "your-super-secret-key-change-in-production"
  GEMINI_API_KEY: "your-gemini-api-key"
  MYSQL_USER: "root"
  MYSQL_PASSWORD: "root"
